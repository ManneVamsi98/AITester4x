| Test ID | Description | Pre-conditions | Steps | Expected Result | Priority |
|---|---|---|---|---|---|
| TC-001 | Create an A/B test with two variations | User is logged in as a CRO Specialist | 1. Navigate to the Experiments page. 2. Click “Create New Test”. 3. Choose “A/B Test”. 4. Define Variation A and Variation B. 5. Save the test. | Test appears in the list with status “Draft” and both variations are listed. | High |
| TC-002 | Verify SmartStats provides Bayesian analysis after test completion | An A/B test with two variations has been launched and reached sufficient traffic | 1. Open the completed test. 2. Click “View Results”. 3. Observe the SmartStats section. | SmartStats displays Bayesian confidence intervals and declares a winner based on statistical validation. | High |
| TC-003 | Use the visual WYSIWYG editor to modify a variation | User is on the edit screen of an existing experiment | 1. Select a variation. 2. Click “Edit with Visual Editor”. 3. Drag‑and‑drop a button onto the page. 4. Save changes. | The variation shows the new button in preview and the change is persisted. | Medium |
| TC-004 | Use the code editor to add custom JavaScript to a variation | User is on the edit screen of an existing experiment | 1. Select a variation. 2. Click “Edit with Code Editor”. 3. Insert a JavaScript snippet that changes button text. 4. Save changes. | The variation executes the script and the button text changes as defined when previewed. | Medium |
| TC-005 | Verify heatmap generation for a page after test runs | An experiment is running on the target page for at least 1 hour | 1. Open the Heatmaps section of the Insights dashboard. 2. Select the page URL. 3. Generate the heatmap. | Heatmap displays click density overlay reflecting user interactions on the page. | High |
| TC-006 | Verify session recordings capture user interactions | An experiment is active on a page with session recording enabled | 1. Open the Session Recordings tab. 2. Play the latest recording. | Recording shows mouse movements, clicks, and scrolls performed by a real user on the page. | High |
| TC-007 | Apply audience targeting based on a behavior attribute | User has access to audience configuration and a defined behavior attribute exists | 1. Open the audience settings of an experiment. 2. Add a rule “Visited product page in last 7 days”. 3. Save the audience. | Experiment is served only to users who satisfy the behavior rule. | High |
| TC-008 | Verify real‑time reporting dashboard updates within 2 seconds | An experiment is running and a conversion event occurs | 1. Trigger a conversion event (e.g., form submit). 2. Open the real‑time dashboard. 3. Observe the metric update. | Conversion count increments on the dashboard within 2 seconds of the event. | High |
| TC-009 | Personalization engine delivers custom content to a segmented user | Personalization campaign is created and segment criteria are defined | 1. Log in as a user who matches the segment. 2. Visit the targeted page. 3. Observe the displayed content. | User sees the personalized content variant as defined in the campaign. | High |
| TC-010 | Integration connector syncs experiment data to Google Analytics | Google Analytics integration is configured and enabled | 1. Complete a conversion in an active experiment. 2. Open Google Analytics. 3. Locate the event data for the experiment. | Corresponding conversion event appears in Google Analytics with correct experiment identifiers. | Medium |
| TC-011 | Collaboration tool assigns a task and displays it on the Kanban board | User is a member of a project with Kanban workflow enabled | 1. Open the experiment’s task panel. 2. Create a task “Review variation B”. 3. Assign the task to a teammate. 4. Save the task. | Task appears on the Kanban board under the assignee’s column with correct title. | Medium |
| TC-012 | Role‑based access prevents non‑admin from deleting an experiment | User is logged in with “Editor” role (no delete permission) | 1. Attempt to delete an existing experiment. | Delete option is disabled or an error message “Insufficient permissions” is shown. | High |
| TC-013 | Two‑factor authentication is required on login | 2FA is enabled for the user account | 1. Enter username and password on the login page. 2. Submit credentials. 3. Provide the second‑factor code. | User is granted access only after entering a valid second‑factor code. | High |
| TC-014 | Activity log records creation of a new experiment | User is logged in with appropriate role | 1. Create a new experiment. 2. Open the Activity Log. | Log entry shows “Experiment <name> created by <user>” with timestamp. | Medium |
| TC-015 | Editing workflow completes within 2 seconds | An experiment is in Draft status | 1. Open the variation editor. 2. Make a minor change (e.g., edit text). 3. Save the change. | Save operation finishes and UI reflects the change within 2 seconds. | High |
| TC-016 | System handles 10,000 concurrent visitors without performance loss | Load testing environment is set up; experiment is active | 1. Simulate 10,000 concurrent users accessing the test page. 2. Measure page load time. | Page load time remains ≤ 2 seconds and no errors are returned. | High |
| TC-017 | GDPR data export request processes successfully | User has personal data stored in the platform | 1. Submit a GDPR data export request via the user settings. 2. Wait for processing. | Export file containing all personal data is generated and downloadable within the stipulated time. | Medium |
| TC-018 | System maintains 99.9 % uptime over a 24‑hour period | Monitoring tools are active | 1. Observe system availability metrics for 24 hours. | Uptime recorded is ≥ 99.9 % with no unplanned downtime exceeding 5 minutes. | High |
| TC-019 | Load test creating 100 experiments in rapid succession | API endpoint for experiment creation is accessible | 1. Send 100 create‑experiment API calls within 1 minute. 2. Track response times. | All 100 experiments are created successfully with average response time ≤ 2 seconds. | Medium |
| TC-020 | Verify that an experiment can be scheduled to start at a future date | User has permission to schedule experiments | 1. Create a new experiment. 2. Set the start date to tomorrow at 09:00 AM. 3. Save the experiment. | Experiment status remains “Scheduled” and automatically switches to “Running” at the specified time. | High |
| TC-021 | Verify split‑URL testing redirects correctly | Split‑URL test is configured with Control URL and Variant URL | 1. Visit the original page URL as a test user. 2. Observe the redirected URL. | User is redirected to either the Control or Variant URL based on allocation logic. | High |
| TC-022 | Verify multivariate test with three variations per element | Multivariate test is defined with two elements, each having three variations | 1. Launch the multivariate test. 2. Collect traffic for a minimum sample size. 3. View results. | All nine possible combinations are tracked and reported correctly. | Medium |
| TC-023 | Verify SmartStats result is statistically validated before winner selection | An experiment has reached the required sample size | 1. Open the results page. 2. Check the SmartStats confidence level. | SmartStats shows a confidence level ≥ 95 % before declaring a winner. | High |
| TC-024 | Verify that a user can pause and resume an ongoing experiment | Experiment is currently running | 1. Click “Pause” on the experiment control panel. 2. Confirm pause action. 3. Click “Resume” after a few minutes. | Experiment stops collecting data while paused and resumes data collection after resume, preserving previous data. | Medium |
| TC-025 | Verify that deletion of an experiment removes associated data from integrations | Experiment is linked to Google Analytics and a CRM connector | 1. Delete the experiment. 2. Check Google Analytics for the experiment’s event data. 3. Check CRM for related records. | All experiment‑related data is removed from both external systems. | Medium |