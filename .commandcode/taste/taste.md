# Taste

- Prefers the agent to work in plan mode for implementation tasks — plan and get approval before executing/writing code. Confidence: 0.9
- Prefers custom slash-command shortcuts for frequent, repetitive workflows (e.g., a `/go` command that runs the whole commit-and-push flow on demand). Confidence: 0.8
- Prefers descriptive, self-explanatory names for custom slash commands over terse abbreviations (e.g., renamed `/go` to `/gotopush` to make the command's purpose explicit). Confidence: 0.5
